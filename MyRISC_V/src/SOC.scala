package MyRISC_V

import chisel3._

class SOC(
    bitness: Int = 64,
    instructionMemorySize: Long = 1 * 1024,
    dataMemorySize: Long = 1 * 1024,
    memoryFile: String = "",
) extends Module {

}
